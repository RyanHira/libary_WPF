<Window x:Class="Bieb.Views.AddOrUpdateAuthorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bieb.Views"
        mc:Ignorable="d"
        Title="AddOrUpdateAuthorView" Height="450" Width="800"
        Background="LightGray">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Author Name:" Margin="10"/>
        <TextBox Text="{Binding Author.Name}" Grid.Row="1" Margin="10" x:Name="txtAuthorName"/>
       


        <Button Grid.Row="6" Margin="10" Width="80" HorizontalAlignment="Center" Content="Save" Command="{Binding SaveCommand}"/>
    </Grid>
</Window>
